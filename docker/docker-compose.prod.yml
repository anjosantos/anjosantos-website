version: "3.9"

services:
  nginx:
    build:
      context: ../
      dockerfile: docker/Dockerfile.prod
    volumes:
      - ../dist:/usr/src/app/dist
      - ../certs:/usr/src/app/certs
      - '../node_modules:/usr/src/app/node_modules'
      - '../public/fonts:/usr/src/app/fonts'
    ports:
      - 80:80
      - 443:443
